# Core (Jacob — data, EDA, infrastructure)
datasets>=2.16.0,<3.0.0  # multi_lexsum still uses script-based loader; not supported in 3.0+
huggingface-hub>=0.20.0
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
pyarrow>=14.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.66.0
jupyter>=1.0.0
ipykernel>=6.25.0
python-pptx>=1.0.0

# NLP utilities (shared)
nltk>=3.8.0
spacy>=3.7.0
scikit-learn>=1.3.0

# Summarization (Yujun) — install on demand
transformers>=4.36.0
torch>=2.1.0
sumy>=0.11.0
rouge-score>=0.1.2
bert-score>=0.3.13
sentencepiece>=0.1.99
accelerate>=0.25.0
openai>=1.0.0

# Classification (Feng)
# - shap is needed in W7 for LR top-token explanations (§3.4)
# - torch is shared with §2 (already declared above)
# - gensim is needed in W8 for self-trained Word2Vec embeddings (§3.5 Bi-LSTM)
shap>=0.44.0
gensim>=4.3.0
# (No TensorFlow — Bi-LSTM is PyTorch to match §2's BART/Legal-BERT stack.)

# Interactive app (Jianong) — install on demand
# gradio>=4.0.0
# bertviz>=1.4.0

# Core (Jacob — data, EDA, infrastructure)
datasets>=2.16.0,<3.0.0  # multi_lexsum still uses script-based loader; not supported in 3.0+
huggingface-hub>=0.20.0
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
pyarrow>=14.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.66.0
jupyter>=1.0.0
ipykernel>=6.25.0

# NLP utilities (shared)
nltk>=3.8.0
spacy>=3.7.0
scikit-learn>=1.3.0

# Summarization (Yujun) — install on demand
# transformers>=4.36.0
# torch>=2.1.0
# sumy>=0.11.0
# rouge-score>=0.1.2
# bert-score>=0.3.13

# Classification deep models (Feng) — install on demand
# tensorflow>=2.15.0
# gensim>=4.3.0
# shap>=0.44.0

# Interactive app (Jianong) — install on demand
# gradio>=4.0.0
# bertviz>=1.4.0
